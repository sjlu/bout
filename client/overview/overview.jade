div.section-header
  | Your Activity
div.section-body
  div.section-column.row
    div.nextsteps(ng-show="!user.has_tracker")
      div.nextsteps-header
        | Next Steps
      a(href="#/connect")
        div.nextsteps-step
          span
            | 1.
          |  Add your fitness tracker
    div.col-sm-2.text-center(ng-repeat="(i, a) in activity", ng-show="user.has_tracker")
      div.panel(ng-class="{'panel-default': i != 0, 'panel-primary': i == 0}")
        div.panel-body
          h2
           | {{a.steps | number}}
        div.panel-footer(ng-class="{'panel-default': i != 0, 'panel-primary': i == 0}")
          span.small
            | {{a.displayDate}}
